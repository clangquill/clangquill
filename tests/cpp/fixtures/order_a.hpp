#pragma once

/// Name that order_b.hpp spells its type with, without including this header.
#define QUILL_ORDER_NAME Tagged

/// Index type order_b.hpp uses, likewise unreachable from that header alone.
using OrderIndex = int;
