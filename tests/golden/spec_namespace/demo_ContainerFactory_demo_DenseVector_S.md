# Class template `demo::ContainerFactory<demo::DenseVector<S>>`

```{cpp:class} template<class S> demo::ContainerFactory<demo::DenseVector<S>>

Container factory for dense vectors.
```

## Method `demo::ContainerFactory::create`

```{cpp:function} template<class S> static demo::DenseVector<S> demo::ContainerFactory<demo::DenseVector<S>>::create(const size_t size)

Create a dense vector.
```
